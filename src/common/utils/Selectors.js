import {useAppSelector} from "../hooks/react-redux-hooks";

// export const isLoggedIn = useAppSelector(state => state.auth.isLoggedIn)
// export const user = useAppSelector(state=>state.profile.userProfile)